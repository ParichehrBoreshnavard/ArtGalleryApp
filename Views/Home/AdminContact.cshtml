@*@using ArtGalleryApp.Models.DataViewModel;
@{
    ViewData["Title"] = "AdminCContact";
    Layout = "_LayoutAdmin";
}

@model List<ArtGalleryApp.Models.DataViewModel.ContactViewModel>

<div class="container w-100 d-flex flex-nowrap justify-content-center pt-5 pb-5  AdminBg  ">
    <div class="Carouselcontainer  w-90 h-90 bg-white  p-5 ">
        <div class="admin-table-header d-flex  flex-nowrap justify-content-space-between">
            <div class=" mb-3 mt-3 mt-md-0 mb-md-0">
                <h4>Contacts Table</h4>
            </div>
           
        </div>
        <a style="background-color: #193C8D" class="btn btn-success" asp-controller="Banners" asp-action="New">Add Banner</a>

        <table class="table table-striped Team">
            <thead>
                <tr>
                    <th>Title</th>
                    <th>Email</th>
                    <th>Subject</th>
                    <th>Body</th>
                    <td>Action</td>
                </tr>
            </thead>
            <tbody>
                @foreach (ContactViewModel row in Model)
                {
                    <tr>

                        <td>@row.Name</td>
                        <td>@row.Email</td>
                        <td>@row.Subject</td>
                        <td>@row.Body</td>
                        <td>
                            <a class="btn btn-primary btn-sm" asp-action="Update" asp-controller="home" asp-route-id="@row.Id">Update</a>
                            <button class="btn btn-danger btn-sm" data-toggle="modal" data-target="#deleteModal">Delete</button>
                        </td>
                    </tr>
                }
            </tbody>

        </table>

    </div>
</div>
*@