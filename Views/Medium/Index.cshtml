@using ArtGalleryApp.Models.DataViewModel;

@{
    ViewData["Title"] = "Index";
    Layout = "_LayoutAdmin";
}
@model ArtGalleryApp.Models.DataViewModel.MediumViewModel
<div class="container  AdminBg  ">
    <div class="container  w-50 bg-white p-5 mt-5 mb-5">
        <h1 class="text-center">Medium Collection</h1>
        <table class="table">
            <thead>
                <tr>
                    <th>Title</th>
                    <th></th>
                </tr>
            </thead>
            <tbody id="art-medium-list">
                @foreach (var row in Model.lstMedium)
                {
                    <tr>
                        <td>@row.Name</td>
                        <td><a class="btn btn-danger btn-sm" asp-action="Delete" asp-controller="medium" asp-route-id="@row.Id">Delete</a></td>
                    </tr>
                }
            </tbody>
        </table>
        <form method="post" asp-controller="medium" asp-action="index">
            <div class="form-group">
                <input class="form-control" asp-for="Name" placeholder="Add a new medium">
                <span style="color:darkred" asp-validation-for="Name"></span>

            </div>
            <button type="submit" class="btn btn-primary float-end" id="add-medium-btn">Add</button>
        </form>
    </div>

</div>


@section Scripts{
    <partial name="_ValidationScriptsPartial"></partial>
}
